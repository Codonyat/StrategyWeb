# Alchemy WebSocket URL (use WSS, not HTTPS)
ALCHEMY_WSS_URL=wss://megaeth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# Contract address (required)
CONTRACT_ADDRESS=0x...

# Port (Railway sets this automatically, defaults to 8080)
PORT=8080

# Frontend URLs for CORS (comma-separated)
FRONTEND_URLS=https://your-frontend.vercel.app,http://localhost:5173
