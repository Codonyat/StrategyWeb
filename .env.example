# GigaETH Strategy Coin Contract Configuration
# Copy this file to .env and fill in your values
# ALL FIELDS ARE REQUIRED - the build will fail if any are missing

# Contract address (deployed Strategy.sol address)
# Must be a valid Ethereum address (0x followed by 40 hex characters)
VITE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# MEGA ERC20 token address (collateral token for minting/bidding)
# Must be a valid Ethereum address
VITE_MEGA_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Chain configuration
VITE_CHAIN_ID=6342
VITE_CHAIN_NAME=MegaETH Testnet
VITE_EXPLORER_URL=https://testnet.megaexplorer.xyz/

# RPC configuration (server-side only - NOT exposed to client)
# This is used by the /api/rpc serverless function
# Do NOT prefix with VITE_ to keep it server-side only
RPC_URL=https://carrot.megaeth.com/rpc

# Subgraph configuration (server-side only - NOT exposed to client)
# These are used by the /api/subgraph serverless function
# Do NOT prefix with VITE_ to keep them server-side only
# Works with TheGraph, Goldsky, or any GraphQL subgraph with Bearer token auth
#
# TheGraph example:
#   SUBGRAPH_URL=https://api.studio.thegraph.com/query/YOUR_ID/YOUR_SUBGRAPH/version/latest
#   Get your API token from: https://thegraph.com/studio/
#
# Goldsky example:
#   SUBGRAPH_URL=https://api.goldsky.com/api/private/project_YOUR_PROJECT_ID/subgraphs/YOUR_SUBGRAPH_NAME/YOUR_VERSION/gn
#   Get your API token from: https://app.goldsky.com/
#
SUBGRAPH_URL=https://api.studio.thegraph.com/query/YOUR_ID/YOUR_SUBGRAPH/version/latest
SUBGRAPH_API_TOKEN=YOUR_API_TOKEN

# Alchemy API key (server-side only - NOT exposed to client)
# Used by the /api/native-price serverless function to fetch MEGA price
# Get your API key from: https://dashboard.alchemy.com/
ALCHEMY_API_KEY=YOUR_ALCHEMY_API_KEY

# Treasury address (receives unclaimed prizes after 7 days)
# Must be a valid Ethereum address
VITE_TREASURY_ADDRESS=0x5000Ff6Cc1864690d947B864B9FB0d603E8d1F1A

# WalletConnect Project ID (get from https://cloud.walletconnect.com)
# Required for RainbowKit to work properly
VITE_WALLETCONNECT_PROJECT_ID=YOUR_PROJECT_ID

# For MegaETH Mainnet, use:
# VITE_CHAIN_ID=6342
# VITE_CHAIN_NAME=MegaETH
# VITE_EXPLORER_URL=https://megaexplorer.xyz
# RPC_URL=https://rpc.megaeth.com

# External links (optional - defaults to '#' if not set)
# VITE_LINK_DOCS=https://docs.example.com
# VITE_LINK_REPOSITORY=https://github.com/example/repo
# VITE_LINK_TWITTER=https://twitter.com/example
# VITE_LINK_TELEGRAM=https://t.me/example
