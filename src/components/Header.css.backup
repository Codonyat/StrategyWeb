.header {
  position: sticky;
  top: 0;
  z-index: 100;
  background-color: var(--color-background-light);
  border-bottom: 1px solid var(--color-border);
}

.header-content {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 var(--spacing-lg);
  height: var(--header-height);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--spacing-md);
  width: 100%;
  box-sizing: border-box;
}

.header-logo {
  text-decoration: none;
  color: var(--color-text-primary);
  font-size: 1.125rem;
  font-weight: 600;
  letter-spacing: -0.02em;
  transition: opacity var(--transition-fast);
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.header-logo:hover {
  opacity: 0.8;
}

.logo-icon {
  width: 48px;
  height: 48px;
  object-fit: contain;
}

.logo-text-container {
  display: flex;
  flex-direction: column;
  gap: 0.15rem;
}

.logo-text {
  color: var(--color-text-primary);
  font-size: 1.25rem;
  font-weight: 700;
  line-height: 1;
  letter-spacing: 0.03em;
  font-family: 'Space Grotesk', 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

.logo-subtitle {
  color: var(--color-text-secondary);
  font-size: 0.7rem;
  font-weight: 500;
  line-height: 1;
  letter-spacing: 0.05em;
  white-space: nowrap;
}

/* Status Chips Container */
.status-chips {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-left: var(--spacing-xl);  /* More breathing room from logo */
}

/* Desktop Navigation */
.nav-desktop {
  display: flex;
  gap: var(--spacing-md);
  flex: 1;
  justify-content: center;
  padding: 0.5rem 1rem;
  background: linear-gradient(180deg,
    rgba(30, 30, 30, 0.6) 0%,
    rgba(20, 20, 20, 0.8) 100%
  );
  border-radius: 24px;
  border: 1px solid transparent;
  background-clip: padding-box;
  position: relative;
  margin: 0 auto;
  max-width: fit-content;
}

/* Inner border with purple to green gradient - muted at top, green at bottom */
.nav-desktop::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 24px;
  padding: 1px;
  background: linear-gradient(180deg,
    rgba(138, 43, 226, 0.15) 0%,
    rgba(0, 255, 136, 0.2) 100%
  );
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  pointer-events: none;
  opacity: 0.6;
}

/* Subtle reflection at bottom */
.nav-desktop::after {
  content: '';
  position: absolute;
  bottom: -6px;
  left: 50%;
  transform: translateX(-50%);
  width: 85%;
  height: 6px;
  background: linear-gradient(180deg,
    rgba(0, 255, 136, 0.05) 0%,
    transparent 100%
  );
  filter: blur(3px);
  pointer-events: none;
  border-radius: 50%;
}

.nav-link {
  text-decoration: none;
  color: rgba(255, 255, 255, 0.5);
  font-size: 0.8125rem;
  font-weight: 500;
  padding: var(--spacing-xs) var(--spacing-sm);
  border-radius: 9999px;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  white-space: nowrap;
  position: relative;
  background: rgba(31, 31, 31, 0.6);
  border: 1px solid transparent;
  letter-spacing: 0.05em;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  opacity: 0.7;
}

.nav-link:hover {
  color: var(--color-text-primary);
  border-color: var(--color-success);
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(0, 255, 136, 0.25);
  opacity: 1;
}

.nav-link:focus-visible {
  outline: 2px solid rgba(0, 255, 136, 0.6);
  outline-offset: 2px;
  border-color: var(--color-success);
  opacity: 1;
}

.nav-link.active {
  color: var(--color-text-primary);
  background-color: var(--color-background-card);
  border: 1px solid rgba(0, 255, 136, 0.6);
  box-shadow: 0 3px 16px rgba(0, 255, 136, 0.4),
              0 0 0 1px rgba(0, 255, 136, 0.3);
  opacity: 1;
}

.nav-link.active::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 50%;
  transform: translateX(-50%);
  width: 60%;
  height: 2px;
  background: linear-gradient(90deg,
    transparent,
    rgba(0, 255, 136, 0.6) 20%,
    rgba(0, 255, 136, 0.8) 50%,
    rgba(0, 255, 136, 0.6) 80%,
    transparent
  );
  filter: blur(2px);
}

.header-right {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  margin-left: auto;
}

/* Custom Wallet Connect Button - Matches CTA design system */
.wallet-connect-btn {
  padding: 0.5rem 1.25rem;
  background: var(--color-background-card);
  border: 2px solid var(--color-border-light);
  border-radius: 9999px;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: 0.875rem;
  position: relative;
  overflow: hidden;
  color: var(--color-text-primary);
  white-space: nowrap;
  min-width: 120px;
}

.wallet-connect-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, var(--color-success), var(--color-secondary));
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 0;
}

.wallet-connect-btn:hover {
  border-color: var(--color-success);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0, 255, 136, 0.3);
}

.wallet-connect-btn:hover::before {
  opacity: 0.1;
}

.wallet-connect-btn:focus-visible {
  outline: 2px solid rgba(0, 255, 136, 0.6);
  outline-offset: 2px;
  border-color: var(--color-success);
}

/* Connected State - Darker pill with subtle green ring */
.wallet-connected-btn {
  padding: 0.5rem 1.25rem;
  background: var(--color-background-light);
  border: 2px solid rgba(0, 255, 136, 0.3);
  border-radius: 9999px;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  position: relative;
  overflow: hidden;
  color: var(--color-text-primary);
  min-width: 120px;
}

.wallet-connected-btn:hover {
  border-color: rgba(0, 255, 136, 0.5);
  background: var(--color-background-card);
  box-shadow: 0 4px 15px rgba(0, 255, 136, 0.2);
}

.wallet-connected-btn:focus-visible {
  outline: 2px solid rgba(0, 255, 136, 0.6);
  outline-offset: 2px;
  border-color: rgba(0, 255, 136, 0.6);
}

.wallet-address-display {
  font-size: 0.875rem;
  font-family: 'Courier New', monospace;
  color: var(--color-text-primary);
}

/* Wrong Network Warning */
.wallet-wrong-network-btn {
  padding: 0.5rem 1.25rem;
  background: rgba(239, 68, 68, 0.1);
  border: 2px solid rgba(239, 68, 68, 0.5);
  border-radius: 9999px;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-weight: 600;
  color: #ef4444;
  font-size: 0.875rem;
  min-width: 120px;
}

.wallet-wrong-network-btn:hover {
  border-color: rgba(239, 68, 68, 0.8);
  background: rgba(239, 68, 68, 0.15);
  box-shadow: 0 4px 15px rgba(239, 68, 68, 0.3);
}

.wallet-wrong-network-btn:focus-visible {
  outline: 2px solid rgba(239, 68, 68, 0.6);
  outline-offset: 2px;
}

.warning-icon {
  font-size: 1rem;
  line-height: 1;
}

/* Mobile Menu Toggle */
.menu-toggle {
  display: none;
  background: none;
  border: none;
  cursor: pointer;
  padding: var(--spacing-xs);
  width: 40px;
  height: 40px;
  position: relative;
}

.menu-icon {
  display: block;
  width: 24px;
  height: 2px;
  background-color: var(--color-text-primary);
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  transition: background-color var(--transition-fast);
}

.menu-icon::before,
.menu-icon::after {
  content: '';
  display: block;
  width: 24px;
  height: 2px;
  background-color: var(--color-text-primary);
  position: absolute;
  left: 0;
  transition: all var(--transition-fast);
}

.menu-icon::before {
  top: -8px;
}

.menu-icon::after {
  bottom: -8px;
}

.menu-icon.open {
  background-color: transparent;
}

.menu-icon.open::before {
  top: 0;
  transform: rotate(45deg);
}

.menu-icon.open::after {
  bottom: 0;
  transform: rotate(-45deg);
}

/* Mobile Navigation */
.nav-mobile {
  display: none;
  flex-direction: column;
  gap: 0.5rem;
  padding: var(--spacing-md);
  position: absolute;
  top: 100%;
  right: 0;
  left: 0;
  width: 100%;
  border: 1px solid var(--color-border);
  border-top: none;
  border-left: none;
  border-right: none;
  background: linear-gradient(180deg,
    rgba(26, 26, 26, 0.98) 0%,
    rgba(20, 20, 20, 0.98) 100%
  );
  backdrop-filter: blur(12px);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
  animation: slideDown 0.25s ease-out;
}

.nav-mobile .nav-link {
  display: block;
  padding: 0.875rem var(--spacing-md);
  width: 100%;
  text-align: center;
  font-size: 0.9rem;
  font-weight: 500;
  opacity: 1;
  background: rgba(31, 31, 31, 0.6);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  letter-spacing: 0.03em;
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

.nav-mobile .nav-link:hover {
  background: rgba(31, 31, 31, 0.9);
  border-color: var(--color-success);
  color: var(--color-text-primary);
  transform: translateX(4px);
}

.nav-mobile .nav-link.active {
  background: rgba(0, 255, 136, 0.08);
  border-color: var(--color-success);
  color: var(--color-text-primary);
  box-shadow: 0 0 12px rgba(0, 255, 136, 0.2);
}

/* Stats in mobile menu - hidden by default, shown at <560px */
.nav-mobile-stats {
  display: none;
  justify-content: center;
  padding-bottom: var(--spacing-sm);
  margin-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--color-border);
}


/* Mobile menu slide-in animation */
@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Responsive */
@media (max-width: 1070px) {
  .nav-desktop {
    display: none;
  }

  .menu-toggle {
    display: block;
  }

  .nav-mobile {
    display: flex;
  }

  .header-content {
    padding: 0 var(--spacing-md);
  }

  .status-chips {
    order: -1;
    margin-left: 0;
    margin-right: auto;
  }

  .logo-text-container {
    display: none;
  }
}

@media (max-width: 600px) {
  .header-content {
    padding: 0 var(--spacing-sm);
    gap: var(--spacing-xs);
  }
}

@media (max-width: 768px) {
  .status-chips {
    gap: 0.4rem;
    margin-right: 0.75rem;
  }
}

@media (max-width: 430px) {
  /* Hide status chips in header, show in mobile menu */
  .status-chips {
    display: none !important;
  }

  .nav-mobile-stats {
    display: flex;
    padding-bottom: var(--spacing-md);
    margin-bottom: var(--spacing-md);
  }

  .nav-mobile {
    padding: var(--spacing-md) var(--spacing-sm);
  }
}

@media (max-width: 480px) {
  .logo-subtitle {
    display: none;
  }

  /* Simplify wallet button on very small screens */
  .wallet-connect-btn {
    padding: 0.5rem 0.875rem;
    font-size: 0.75rem;
    min-width: 100px;
  }

  .wallet-connected-btn {
    padding: 0.5rem 0.875rem;
    min-width: 100px;
  }

  .wallet-address-display {
    font-size: 0.75rem;
  }

  .wallet-wrong-network-btn {
    padding: 0.5rem 0.875rem;
    font-size: 0.75rem;
    min-width: 100px;
  }
}
